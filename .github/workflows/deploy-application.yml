name: Show Me the S3cr3tz
on: [push]

jobs:
  debug:
    name: Debug
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Set up secret file
        env:
          ROYALTY_SERVER_SSH: ${{ secrets.ROYALTY_SERVER_SSH }}
        run: |
          echo $ROYALTY_SERVER_SSH >> secrets.txt

      - name: Run tmate
        uses: mxschmitt/action-tmate@v2
